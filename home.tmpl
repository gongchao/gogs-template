{{template "base/head" .}}
<div class="home">
	<div class="ui stackable middle very relaxed page grid">
		<div class="sixteen wide center aligned centered column">
			<div>
				<img class="logo" src="{{AppSubUrl}}/img/gogs-lg.png" />
			</div>
			<div class="hero">
				<h2>代码管理仓库</h2>
				<p>Code management warehouse</p>
			</div>
		</div>
	</div>
</div>
{{template "base/footer" .}}
